#include "pooling_layer.h"

Tensor maxPooling2D(const Tensor& input, int pool_height, int pool_width, int stride, int padding) {
    // Max pooling implementation
}

Tensor avgPooling2D(const Tensor& input, int pool_height, int pool_width, int stride, int padding) {
    // Average pooling implementation
}
